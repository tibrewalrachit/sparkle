/-
  Veil AXI — SMT-checked transition-system models of the AMBA AXI
  write/read channels (ARM IHI 0022 Issue L), cross-verifying the
  interactively-proven spec model in Sparkle/Verification/AXIProps.lean.
-/

import VeilAXI.WriteChannel
import VeilAXI.ReadChannel
