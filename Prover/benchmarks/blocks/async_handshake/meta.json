{
  "top": "async_handshake",
  "category": "handshake",
  "safe": {
    "file": "design.sv",
    "expect": "safe"
  },
  "bugs": [
    {
      "file": "bug1.sv",
      "expect": "bug",
      "min_depth": 5,
      "description": "Return-to-zero race: the sender leaves its WAIT phase without waiting for ack to fall, so a new transfer can overwrite the held data while the receiver is still releasing ack."
    }
  ],
  "description": "4-phase req/ack handshake between a sender FSM and a slow-releasing receiver; asserts phase invariants and data stability while ack is high.",
  "state_bits": 24
}
