/-
  Auto-translated from behavioral SystemVerilog by sparkle_fv.translate (Verilator XML AST front end)
  Source     : benchmarks/blocks/sync_fifo/design.sv
  Top module : sync_fifo
  Proof obligations extracted from immediate `assert`
  statements; combinational nets are inlined so each property
  mentions only registers and primary inputs.
-/

import Sparkle
import Sparkle.Verification.Temporal

open Sparkle.Core.Domain
open Sparkle.Core.Signal
open Sparkle.Verification.Temporal

namespace SparkleFV.Sync_fifoProps

/-- Property `prop_1` (safety); source: design.sv:63 `assert (count <= DEPTH);`
    The condition must hold at every cycle. -/
def prop_1 {dom : DomainConfig}
    (count : Signal dom (BitVec 4)) : Signal dom Bool :=
  (~~~((BitVec.ult · ·) <$> (Signal.pure 8#4) <*> count))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_1 (kind: safety, src: design.sv:63)
--   design registers  : wr_ptr : BitVec 3, rd_ptr : BitVec 3, count : BitVec 4
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Sync_fifo.sync_fifo` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_1, sync_fifo]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_1
def candidateInv_prop_1 : Prop := True
-- INVARIANT-SLOT-END prop_1

/-- Safety obligation: `prop_1` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem sync_fifo_prop_1 {dom : DomainConfig}
    (count : Signal dom (BitVec 4)) :
    always (prop_1 count) := by
  sorry

/-- Property `prop_2` (safety); source: design.sv:66 `assert (count[2:0] == (wr_ptr - rd_ptr));`
    The condition must hold at every cycle. -/
def prop_2 {dom : DomainConfig}
    (count : Signal dom (BitVec 4)) (wr_ptr : Signal dom (BitVec 3)) (rd_ptr : Signal dom (BitVec 3)) : Signal dom Bool :=
  (((BitVec.extractLsb' 0 3) <$> count) === (wr_ptr - rd_ptr))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_2 (kind: safety, src: design.sv:66)
--   design registers  : wr_ptr : BitVec 3, rd_ptr : BitVec 3, count : BitVec 4
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Sync_fifo.sync_fifo` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_2, sync_fifo]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_2
def candidateInv_prop_2 : Prop := True
-- INVARIANT-SLOT-END prop_2

/-- Safety obligation: `prop_2` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem sync_fifo_prop_2 {dom : DomainConfig}
    (count : Signal dom (BitVec 4)) (wr_ptr : Signal dom (BitVec 3)) (rd_ptr : Signal dom (BitVec 3)) :
    always (prop_2 count wr_ptr rd_ptr) := by
  sorry

/-- Property `prop_3` (safety); source: design.sv:68 `assert (!(deq_valid && count == 4'd0));`
    The condition must hold at every cycle. -/
def prop_3 {dom : DomainConfig}
    (count : Signal dom (BitVec 4)) : Signal dom Bool :=
  (~~~((~~~((Signal.pure 0#4) === count)) &&& ((Signal.pure 0#4) === count)))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_3 (kind: safety, src: design.sv:68)
--   design registers  : wr_ptr : BitVec 3, rd_ptr : BitVec 3, count : BitVec 4
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Sync_fifo.sync_fifo` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_3, sync_fifo]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_3
def candidateInv_prop_3 : Prop := True
-- INVARIANT-SLOT-END prop_3

/-- Safety obligation: `prop_3` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem sync_fifo_prop_3 {dom : DomainConfig}
    (count : Signal dom (BitVec 4)) :
    always (prop_3 count) := by
  sorry

/-- Property `prop_4` (safety); source: design.sv:70 `assert (!(enq_ready && count == DEPTH));`
    The condition must hold at every cycle. -/
def prop_4 {dom : DomainConfig}
    (count : Signal dom (BitVec 4)) : Signal dom Bool :=
  (~~~((~~~((Signal.pure 8#4) === count)) &&& ((Signal.pure 8#4) === count)))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_4 (kind: safety, src: design.sv:70)
--   design registers  : wr_ptr : BitVec 3, rd_ptr : BitVec 3, count : BitVec 4
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Sync_fifo.sync_fifo` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_4, sync_fifo]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_4
def candidateInv_prop_4 : Prop := True
-- INVARIANT-SLOT-END prop_4

/-- Safety obligation: `prop_4` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem sync_fifo_prop_4 {dom : DomainConfig}
    (count : Signal dom (BitVec 4)) :
    always (prop_4 count) := by
  sorry

end SparkleFV.Sync_fifoProps
