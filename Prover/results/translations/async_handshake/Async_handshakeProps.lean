/-
  Auto-translated from behavioral SystemVerilog by sparkle_fv.translate (Verilator XML AST front end)
  Source     : benchmarks/blocks/async_handshake/design.sv
  Top module : async_handshake
  Proof obligations extracted from immediate `assert`
  statements; combinational nets are inlined so each property
  mentions only registers and primary inputs.
-/

import Sparkle
import Sparkle.Verification.Temporal

open Sparkle.Core.Domain
open Sparkle.Core.Signal
open Sparkle.Verification.Temporal

namespace SparkleFV.Async_handshakeProps

/-- Property `prop_1` (safety); source: design.sv:97 `assert (req == (sstate == S_REQ));`
    The condition must hold at every cycle. -/
def prop_1 {dom : DomainConfig}
    (req : Signal dom Bool) (sstate : Signal dom (BitVec 2)) : Signal dom Bool :=
  (req === ((Signal.pure 1#2) === sstate))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_1 (kind: safety, src: design.sv:97)
--   design registers  : sstate : BitVec 2, req : Bool, data_r : BitVec 8, rstate : BitVec 2, ack : Bool, cap : BitVec 8, rel_cnt : BitVec 2
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Async_handshake.async_handshake` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_1, async_handshake]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_1
def candidateInv_prop_1 : Prop := True
-- INVARIANT-SLOT-END prop_1

/-- Safety obligation: `prop_1` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem async_handshake_prop_1 {dom : DomainConfig}
    (req : Signal dom Bool) (sstate : Signal dom (BitVec 2)) :
    always (prop_1 req sstate) := by
  sorry

/-- Property `prop_2` (safety); source: design.sv:98 `assert (ack == (rstate != R_IDLE));`
    The condition must hold at every cycle. -/
def prop_2 {dom : DomainConfig}
    (ack : Signal dom Bool) (rstate : Signal dom (BitVec 2)) : Signal dom Bool :=
  (ack === (~~~((Signal.pure 0#2) === rstate)))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_2 (kind: safety, src: design.sv:98)
--   design registers  : sstate : BitVec 2, req : Bool, data_r : BitVec 8, rstate : BitVec 2, ack : Bool, cap : BitVec 8, rel_cnt : BitVec 2
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Async_handshake.async_handshake` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_2, async_handshake]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_2
def candidateInv_prop_2 : Prop := True
-- INVARIANT-SLOT-END prop_2

/-- Safety obligation: `prop_2` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem async_handshake_prop_2 {dom : DomainConfig}
    (ack : Signal dom Bool) (rstate : Signal dom (BitVec 2)) :
    always (prop_2 ack rstate) := by
  sorry

/-- Property `prop_3` (safety); source: design.sv:102 `assert (cap == data_r);`
    The condition must hold at every cycle. -/
def prop_3 {dom : DomainConfig}
    (ack : Signal dom Bool) (cap : Signal dom (BitVec 8)) (data_r : Signal dom (BitVec 8)) : Signal dom Bool :=
  (~~~(ack &&& (~~~(cap === data_r))))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_3 (kind: safety, src: design.sv:102)
--   design registers  : sstate : BitVec 2, req : Bool, data_r : BitVec 8, rstate : BitVec 2, ack : Bool, cap : BitVec 8, rel_cnt : BitVec 2
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Async_handshake.async_handshake` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_3, async_handshake]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_3
def candidateInv_prop_3 : Prop := True
-- INVARIANT-SLOT-END prop_3

/-- Safety obligation: `prop_3` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem async_handshake_prop_3 {dom : DomainConfig}
    (ack : Signal dom Bool) (cap : Signal dom (BitVec 8)) (data_r : Signal dom (BitVec 8)) :
    always (prop_3 ack cap data_r) := by
  sorry

end SparkleFV.Async_handshakeProps
