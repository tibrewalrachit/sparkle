/-
  Auto-translated from behavioral SystemVerilog by sparkle_fv.translate (Verilator XML AST front end)
  Source     : benchmarks/blocks/gray_counter/design.sv
  Top module : gray_counter
  Proof obligations extracted from immediate `assert`
  statements; combinational nets are inlined so each property
  mentions only registers and primary inputs.
-/

import Sparkle
import Sparkle.Verification.Temporal

open Sparkle.Core.Domain
open Sparkle.Core.Signal
open Sparkle.Verification.Temporal

namespace SparkleFV.Gray_counterProps

/-- Property `prop_1` (safety); source: design.sv:47 `assert (gray == (bin ^ (bin >> 1)));`
    The condition must hold at every cycle. -/
def prop_1 {dom : DomainConfig}
    (gray : Signal dom (BitVec 8)) (bin : Signal dom (BitVec 8)) : Signal dom Bool :=
  (gray === (bin ^^^ ((fun x y => x >>> y.toNat) <$> bin <*> (Signal.pure 1#32))))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_1 (kind: safety, src: design.sv:47)
--   design registers  : bin : BitVec 8, gray : BitVec 8, gray_prev : BitVec 8, moved : Bool
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Gray_counter.gray_counter` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_1, gray_counter]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_1
def candidateInv_prop_1 : Prop := True
-- INVARIANT-SLOT-END prop_1

/-- Safety obligation: `prop_1` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem gray_counter_prop_1 {dom : DomainConfig}
    (gray : Signal dom (BitVec 8)) (bin : Signal dom (BitVec 8)) :
    always (prop_1 gray bin) := by
  sorry

/-- Property `prop_2` (safety); source: design.sv:50 `assert (diff_onehot);`
    The condition must hold at every cycle. -/
def prop_2 {dom : DomainConfig}
    (moved : Signal dom Bool) (gray : Signal dom (BitVec 8)) (gray_prev : Signal dom (BitVec 8)) : Signal dom Bool :=
  (~~~(moved &&& (~~~((~~~((Signal.pure 0#8) === (gray ^^^ gray_prev))) &&& ((Signal.pure 0#8) === ((gray ^^^ gray_prev) &&& ((gray ^^^ gray_prev) - (Signal.pure 1#8))))))))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_2 (kind: safety, src: design.sv:50)
--   design registers  : bin : BitVec 8, gray : BitVec 8, gray_prev : BitVec 8, moved : Bool
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Gray_counter.gray_counter` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_2, gray_counter]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_2
def candidateInv_prop_2 : Prop := True
-- INVARIANT-SLOT-END prop_2

/-- Safety obligation: `prop_2` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem gray_counter_prop_2 {dom : DomainConfig}
    (moved : Signal dom Bool) (gray : Signal dom (BitVec 8)) (gray_prev : Signal dom (BitVec 8)) :
    always (prop_2 moved gray gray_prev) := by
  sorry

end SparkleFV.Gray_counterProps
