/-
  Auto-translated from behavioral SystemVerilog by sparkle_fv.translate (Verilator XML AST front end)
  Source     : benchmarks/blocks/uart_tx/design.sv
  Top module : uart_tx
  Proof obligations extracted from immediate `assert`
  statements; combinational nets are inlined so each property
  mentions only registers and primary inputs.
-/

import Sparkle
import Sparkle.Verification.Temporal

open Sparkle.Core.Domain
open Sparkle.Core.Signal
open Sparkle.Verification.Temporal

namespace SparkleFV.Uart_txProps

/-- Property `prop_1` (safety); source: design.sv:91 `assert (state == ST_IDLE || state == ST_START ||`
    The condition must hold at every cycle. -/
def prop_1 {dom : DomainConfig}
    (state_v : Signal dom (BitVec 4)) : Signal dom Bool :=
  (((((Signal.pure 1#4) === state_v) ||| ((Signal.pure 2#4) === state_v)) ||| ((Signal.pure 4#4) === state_v)) ||| ((Signal.pure 8#4) === state_v))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_1 (kind: safety, src: design.sv:91)
--   design registers  : state_v : BitVec 4, baud : BitVec 3, bitidx : BitVec 3, sh : BitVec 8
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Uart_tx.uart_tx` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_1, uart_tx]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_1
def candidateInv_prop_1 : Prop := True
-- INVARIANT-SLOT-END prop_1

/-- Safety obligation: `prop_1` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem uart_tx_prop_1 {dom : DomainConfig}
    (state_v : Signal dom (BitVec 4)) :
    always (prop_1 state_v) := by
  sorry

/-- Property `prop_2` (safety); source: design.sv:94 `assert (baud <= DIV);`
    The condition must hold at every cycle. -/
def prop_2 {dom : DomainConfig}
    (baud : Signal dom (BitVec 3)) : Signal dom Bool :=
  (~~~((BitVec.ult · ·) <$> (Signal.pure 3#3) <*> baud))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_2 (kind: safety, src: design.sv:94)
--   design registers  : state_v : BitVec 4, baud : BitVec 3, bitidx : BitVec 3, sh : BitVec 8
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Uart_tx.uart_tx` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_2, uart_tx]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_2
def candidateInv_prop_2 : Prop := True
-- INVARIANT-SLOT-END prop_2

/-- Safety obligation: `prop_2` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem uart_tx_prop_2 {dom : DomainConfig}
    (baud : Signal dom (BitVec 3)) :
    always (prop_2 baud) := by
  sorry

/-- Property `prop_3` (safety); source: design.sv:97 `assert (tx == 1'b1);`
    The condition must hold at every cycle. -/
def prop_3 {dom : DomainConfig}
    (state_v : Signal dom (BitVec 4)) (sh : Signal dom (BitVec 8)) : Signal dom Bool :=
  (~~~(((Signal.pure 1#4) === state_v) &&& (~~~((~~~((Signal.pure 2#4) === state_v)) &&& ((~~~((Signal.pure 4#4) === state_v)) ||| ((·.getLsbD 0) <$> sh))))))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_3 (kind: safety, src: design.sv:97)
--   design registers  : state_v : BitVec 4, baud : BitVec 3, bitidx : BitVec 3, sh : BitVec 8
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Uart_tx.uart_tx` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_3, uart_tx]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_3
def candidateInv_prop_3 : Prop := True
-- INVARIANT-SLOT-END prop_3

/-- Safety obligation: `prop_3` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem uart_tx_prop_3 {dom : DomainConfig}
    (state_v : Signal dom (BitVec 4)) (sh : Signal dom (BitVec 8)) :
    always (prop_3 state_v sh) := by
  sorry

/-- Property `prop_4` (safety); source: design.sv:98 `assert (baud == 3'd0);`
    The condition must hold at every cycle. -/
def prop_4 {dom : DomainConfig}
    (state_v : Signal dom (BitVec 4)) (baud : Signal dom (BitVec 3)) : Signal dom Bool :=
  (~~~(((Signal.pure 1#4) === state_v) &&& (~~~((Signal.pure 0#3) === baud))))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_4 (kind: safety, src: design.sv:98)
--   design registers  : state_v : BitVec 4, baud : BitVec 3, bitidx : BitVec 3, sh : BitVec 8
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Uart_tx.uart_tx` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_4, uart_tx]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_4
def candidateInv_prop_4 : Prop := True
-- INVARIANT-SLOT-END prop_4

/-- Safety obligation: `prop_4` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem uart_tx_prop_4 {dom : DomainConfig}
    (state_v : Signal dom (BitVec 4)) (baud : Signal dom (BitVec 3)) :
    always (prop_4 state_v baud) := by
  sorry

/-- Property `prop_5` (safety); source: design.sv:102 `assert (tx == 1'b0);`
    The condition must hold at every cycle. -/
def prop_5 {dom : DomainConfig}
    (state_v : Signal dom (BitVec 4)) (sh : Signal dom (BitVec 8)) : Signal dom Bool :=
  (~~~(((Signal.pure 2#4) === state_v) &&& ((~~~((Signal.pure 2#4) === state_v)) &&& ((~~~((Signal.pure 4#4) === state_v)) ||| ((·.getLsbD 0) <$> sh)))))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_5 (kind: safety, src: design.sv:102)
--   design registers  : state_v : BitVec 4, baud : BitVec 3, bitidx : BitVec 3, sh : BitVec 8
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Uart_tx.uart_tx` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_5, uart_tx]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_5
def candidateInv_prop_5 : Prop := True
-- INVARIANT-SLOT-END prop_5

/-- Safety obligation: `prop_5` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem uart_tx_prop_5 {dom : DomainConfig}
    (state_v : Signal dom (BitVec 4)) (sh : Signal dom (BitVec 8)) :
    always (prop_5 state_v sh) := by
  sorry

/-- Property `prop_6` (safety); source: design.sv:105 `assert (tx == 1'b1);`
    The condition must hold at every cycle. -/
def prop_6 {dom : DomainConfig}
    (state_v : Signal dom (BitVec 4)) (sh : Signal dom (BitVec 8)) : Signal dom Bool :=
  (~~~(((Signal.pure 8#4) === state_v) &&& (~~~((~~~((Signal.pure 2#4) === state_v)) &&& ((~~~((Signal.pure 4#4) === state_v)) ||| ((·.getLsbD 0) <$> sh))))))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_6 (kind: safety, src: design.sv:105)
--   design registers  : state_v : BitVec 4, baud : BitVec 3, bitidx : BitVec 3, sh : BitVec 8
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Uart_tx.uart_tx` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_6, uart_tx]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_6
def candidateInv_prop_6 : Prop := True
-- INVARIANT-SLOT-END prop_6

/-- Safety obligation: `prop_6` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem uart_tx_prop_6 {dom : DomainConfig}
    (state_v : Signal dom (BitVec 4)) (sh : Signal dom (BitVec 8)) :
    always (prop_6 state_v sh) := by
  sorry

end SparkleFV.Uart_txProps
