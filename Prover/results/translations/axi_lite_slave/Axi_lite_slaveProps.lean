/-
  Auto-translated from behavioral SystemVerilog by sparkle_fv.translate (Verilator XML AST front end)
  Source     : benchmarks/blocks/axi_lite_slave/design.sv
  Top module : axi_lite_slave
  Proof obligations extracted from immediate `assert`
  statements; combinational nets are inlined so each property
  mentions only registers and primary inputs.
-/

import Sparkle
import Sparkle.Verification.Temporal

open Sparkle.Core.Domain
open Sparkle.Core.Signal
open Sparkle.Verification.Temporal

namespace SparkleFV.Axi_lite_slaveProps

/-- Property `prop_1` (safety); source: design.sv:88 `assert (aw_done && w_done);`
    The condition must hold at every cycle. -/
def prop_1 {dom : DomainConfig}
    (bvalid_q : Signal dom Bool) (aw_done : Signal dom Bool) (w_done : Signal dom Bool) : Signal dom Bool :=
  (~~~(bvalid_q &&& (~~~(aw_done &&& w_done))))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_1 (kind: safety, src: design.sv:88)
--   design registers  : aw_done : Bool, w_done : Bool, bvalid_q : Bool, awaddr_q : BitVec 4, wdata_q : BitVec 32, prev_ok : Bool, bvalid_p : Bool, bready_p : Bool
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Axi_lite_slave.axi_lite_slave` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_1, axi_lite_slave]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_1
def candidateInv_prop_1 : Prop := True
-- INVARIANT-SLOT-END prop_1

/-- Safety obligation: `prop_1` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem axi_lite_slave_prop_1 {dom : DomainConfig}
    (bvalid_q : Signal dom Bool) (aw_done : Signal dom Bool) (w_done : Signal dom Bool) :
    always (prop_1 bvalid_q aw_done w_done) := by
  sorry

/-- Property `prop_2` (safety); source: design.sv:91 `assert (bvalid);`
    The condition must hold at every cycle. -/
def prop_2 {dom : DomainConfig}
    (prev_ok : Signal dom Bool) (rst : Signal dom Bool) (bvalid_p : Signal dom Bool) (bready_p : Signal dom Bool) (bvalid_q : Signal dom Bool) : Signal dom Bool :=
  (~~~((((prev_ok &&& (~~~rst)) &&& bvalid_p) &&& (~~~bready_p)) &&& (~~~bvalid_q)))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_2 (kind: safety, src: design.sv:91)
--   design registers  : aw_done : Bool, w_done : Bool, bvalid_q : Bool, awaddr_q : BitVec 4, wdata_q : BitVec 32, prev_ok : Bool, bvalid_p : Bool, bready_p : Bool
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Axi_lite_slave.axi_lite_slave` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_2, axi_lite_slave]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_2
def candidateInv_prop_2 : Prop := True
-- INVARIANT-SLOT-END prop_2

/-- Safety obligation: `prop_2` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem axi_lite_slave_prop_2 {dom : DomainConfig}
    (prev_ok : Signal dom Bool) (rst : Signal dom Bool) (bvalid_p : Signal dom Bool) (bready_p : Signal dom Bool) (bvalid_q : Signal dom Bool) :
    always (prop_2 prev_ok rst bvalid_p bready_p bvalid_q) := by
  sorry

/-- Property `prop_3` (safety); source: design.sv:93 `assert (!(bvalid && (awready || wready)));`
    The condition must hold at every cycle. -/
def prop_3 {dom : DomainConfig}
    (bvalid_q : Signal dom Bool) (aw_done : Signal dom Bool) (w_done : Signal dom Bool) : Signal dom Bool :=
  (~~~(bvalid_q &&& (((~~~aw_done) &&& (~~~bvalid_q)) ||| ((~~~w_done) &&& (~~~bvalid_q)))))

-- PROOF PLAN (auto-generated by sparkle-fv):
--   property          : prop_3 (kind: safety, src: design.sv:93)
--   design registers  : aw_done : Bool, w_done : Bool, bvalid_q : Bool, awaddr_q : BitVec 4, wdata_q : BitVec 32, prev_ok : Bool, bvalid_p : Bool, bready_p : Bool
--   binding           : instantiate the parameters with the signals produced
--                       by `SparkleFV.Axi_lite_slave.axi_lite_slave` (project the state loop),
--                       then unfold and prove by induction over time.
--   suggested tactics : `intro t`; `simp [prop_3, axi_lite_slave]`;
--                       strengthen with an inductive invariant if the property
--                       is not inductive as stated (invariant slot below).
-- INVARIANT-SLOT-BEGIN prop_3
def candidateInv_prop_3 : Prop := True
-- INVARIANT-SLOT-END prop_3

/-- Safety obligation: `prop_3` holds at every cycle of the design.
    NOTE: as stated the parameters are universally quantified; the AI prover
    threads the actual design binding here before discharging the goal. -/
theorem axi_lite_slave_prop_3 {dom : DomainConfig}
    (bvalid_q : Signal dom Bool) (aw_done : Signal dom Bool) (w_done : Signal dom Bool) :
    always (prop_3 bvalid_q aw_done w_done) := by
  sorry

end SparkleFV.Axi_lite_slaveProps
